# Example: customize payload size, iterations, and core frequency (e.g. 100 MHz)
make WOLFSSL_ROOT=/path/to/wolfssl BENCH_PAYLOAD_SIZE=4096 ITERATIONS=100 CPU_FREQ_MHZ=100.0

git clone https://github.com/wolfSSL/wolfssl.git wolfssl

# 1. Build Experiment 1 (Main 4 KB)
make clean && make BENCH_PAYLOAD_SIZE=4096

# 2. Build Experiment 2 (Small 64 B header)
make clean && make BENCH_PAYLOAD_SIZE=64

# 3. Build Experiment 3 (16 KB firmware binary)
make clean && make BENCH_PAYLOAD_SIZE=16384 ITERATIONS=20

# 4. Host Simulation / Verification (Run directly on PC / WSL)
make clean && make CROSS_COMPILE= ARCH_FLAGS= && ./build/hash_bench.elf
